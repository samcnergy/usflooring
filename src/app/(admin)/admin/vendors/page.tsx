// PHASE 2 placeholder. Phase 1 auto-creates Vendor rows on first use.

export default function VendorsPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold text-brand-700">Vendors</h1>
      <p className="mt-2 text-marble-700">Coming in Phase 2.</p>
      <p className="mt-2 text-sm text-marble-700">
        Phase 1 auto-creates a <code>Vendor</code> row the first time a vendor name
        is typed on a Vendor PO. Phase 2 adds CRUD, contacts, and default
        delivery addresses on this page.
      </p>
    </div>
  );
}
