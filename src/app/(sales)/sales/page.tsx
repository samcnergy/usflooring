export default function NewOrderPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold text-brand-700">New Order</h1>
      <p className="mt-2 text-marble-700">
        The full Invoice form (with mandatory salesperson dropdown, area table,
        category checkboxes, totals, etc.) ships in build step 3.
      </p>
    </div>
  );
}
